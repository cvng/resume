// Stub for "markdownresume/src/app/api/generate-pdf/route" (see tsconfig paths).
// The real module is Next.js app source that doesn't typecheck standalone,
// so tsc is pointed here instead. The route expects a NextRequest, hence the
// `as never` cast at the call site.

export declare function POST(request: never): Promise<Response>;
