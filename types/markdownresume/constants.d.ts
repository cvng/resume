// Stub for "markdownresume/src/lib/constants" (see tsconfig paths).
// Mirrors the ThemeProps interface from the real module.

export interface ThemeProps {
    fontName: string;
    fontScale: number;
    headingScale: number;
    lineHeightScale: number;
    xPaddingScale: number;
    yPaddingScale: number;
    headerColor: string;
    textColor: string;
    linkColor: string;
}

export declare const themes: { [key: string]: ThemeProps };
